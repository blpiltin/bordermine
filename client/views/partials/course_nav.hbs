<nav class="navbar navbar-expand-lg navbar-dark bg-primary rounded-bottom cm-view-course-nav">
  <a class="navbar-brand" id="lbl-logo" href="{{view.ownerPath}}/{{course.slug}}">
    <div class="cm-view-course-name">{{course.name}}</div>
    <div class="cm-view-course-tagline">{{#if course.tagline }}{{ course.tagline }}{{/if}}</div>
  </a>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{view.ownerPath}}/{{course.slug}}">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{view.ownerPath}}/{{course.slug}}/about">About</a>
      </li>
    </ul>
  </div>
</nav>